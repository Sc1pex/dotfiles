# /var/home/scipex/.config/tmuxinator/pclp-tema3.yml

name: pclp-tema3
root: ~/dev/pclp/tema3/

windows:
  - nvim:
      layout: main-vertical
      panes:
        - "nvim -c ':SessionManager load_current_dir_session'"
  - run:
      layout: even-horizontal
      panes:
        - distrobox enter tema3-pclp

