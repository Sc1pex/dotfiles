# /var/home/scipex/.config/tmuxinator/dotfiles.yml

name: dotfiles
root: ~/.dotfiles/

windows:
  - nvim:
      layout: main-vertical
      panes:
        - "nvim -c ':SessionManager load_current_dir_session'"
