# /var/home/scipex/.config/tmuxinator/nvim.yml

name: nvim
root: ~/.dotfiles/.config/nvim/

windows:
  - nvim:
      layout: main-vertical
      panes:
        - "nvim -c ':SessionManager load_current_dir_session'"
