# /var/home/scipex/.config/tmuxinator/scorwhist.yml

name: scorwhist
root: ~/dev/Projects/scorwhist

windows:
  - nvim:
      layout: main-vertical
      panes:
        - "nvim -c ':SessionManager load_current_dir_session'"
  - run:
      layout: even-horizontal
      panes:
        - bun run dev
